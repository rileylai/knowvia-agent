# Knowvia Agent Project Roadmap

本文件只記錄 development priority、交付結果與驗證方式。詳細 implementation
behavior 應放在相關 `docs/` 與 just-in-time SDD spec。

Phase 0 完成後，每個主要 feature 都先切成可人工操作的 end-to-end vertical
slice。Automated test 不能取代 frontend manual verification。未完成所需人工驗收
時不可標記 `done`。新發現的 follow-up 使用 `N.1`、`N.1.1` 等新 ID append，不重編
既有 roadmap。Status 只使用固定 vocabulary，純 foundation 或 documentation work
可依自己的 Verification contract 在沒有 frontend gate 的情況下完成。

## Full Roadmap

| ID | Phase / Slice | Status | Goal | Deliverable | Verification | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| 0.1 | Documentation Baseline | done | 建立 Knowvia source of truth、Roadmap、Decisions 與 repository instructions。 | `docs/` baseline、`dev_state/` 三份文件、精簡 `AGENTS.md`。 | Automated: path、state layout 與 diff checks pass.<br>Manual: Documentation structurally reviewed. | 純 documentation work，不需要 frontend gate。 |
| 0.2 | Foundation Runtime Cleanup | done | 斷開 inherited LearnLoop runtime dependency，保留 Knowvia backend baseline。 | Telegram、Supplement、Notion writer、Redis/RQ core readiness dependency disconnected；Notion indexing、QA、source ingestion preserved。 | Automated: foundation regression and readiness checks pass.<br>Manual: Backend baseline independently verified; frontend not required. | Automated checks and backend manual verification complete. 未開始新的 frontend feature。 |
| 0.2.1 | Knowvia Local Runtime Isolation | done | 將 active local PostgreSQL 與 LearnLoop data 分開。 | `knowvia-postgres`、dedicated volume、`knowvia` role/database 與更新後的 local config。 | Automated: migration、pgvector、readiness 與 database identity checks pass.<br>Manual: Docker state and preserved legacy data reviewed. | Local verification 與前一階段人工驗收完成；舊 LearnLoop container、bind mount 與資料保留。 |
| 1.0 | Thin Frontend Harness | done | 提供後續每個 slice 的最小人工驗收入口。 | Frontend shell + Knowledge、Chat、Memory surfaces；Memory 先是 placeholder。 | Automated: API connection、loading、success、error 與 Chat baseline tests pass.<br>Manual: Chat baseline request → loading/success/error visible. | Automated tests、production build、startup probe 與人工驗收已通過。 |
| 1.0.1 | Chat Keyboard Submission | done | 讓 textarea 以 Enter 送出，Shift+Enter 保留換行。 | IME-safe keyboard submit，不改變 button、loading 或 error flow。 | Automated: Enter、Shift+Enter、IME、empty query 與 duplicate-submit tests pass.<br>Manual: Keyboard behavior reviewed in browser. | Automated tests 與人工驗收已通過。 |
| 1.0.2 | Insufficient Info Citation Contract | done | 確保 evidence 不足時不附 citation。 | Backend 保證 `insufficient_info=true` 時 `citations=[]`；frontend 清除前次結果。 | Automated: orchestrator、API 與 frontend regression pass.<br>Manual: `hi` 顯示 insufficient info 與 zero citations。 | Local probe 與人工驗收已通過；未調整 retrieval architecture 或 ranking。 |
| 2.0 | Generic Knowledge Contract | done | 定義所有 source 共用的最小 ingestion、chunk、embedding、retrieval 與 citation contract。 | Generic `SourceDocument`、`KnowledgeChunk`、retrieval eligibility 與 backend citation contract，支援 `notion`、`pdf`。 | Automated: Contract、eligibility、retrieval、citation 與 Notion regression passes.<br>Manual: Combined with 2.1 source flow. | Automated implementation、local indexed PDF retrieval、bounded QA 與 browser 人工驗收已完成。 |
| 2.1 | PDF Knowledge Pipeline | done | 先完成一條可驗收的 generic source path。 | PDF → `SourceDocument` → `KnowledgeChunk` → embedding → retrieval → citation → frontend verification。 | Automated: PDF indexing, retrieval and citation regression passes.<br>Manual: Upload PDF → indexing → Chat question → citation. | `2.1.1`、`2.1.2` 與 `2.1.3` 均已完成人工驗收。 |
| 2.1.1 | PDF Positive QA Retrieval/Sufficiency Fix | done | 確保 PDF enumeration question 有足夠 generic retrieval context，且不放寬 `insufficient_info` guardrail。 | QA default retrieval coverage 調整為 `top_k=10`；保留 PDF eligibility、backend citation 與 zero-citation invariant。 | Automated: PDF QA default top-k positive contract、irrelevant evidence insufficient-info regression passes.<br>Manual: A/B/C PDF probes and Chat positive questions. | Automated regression 與 A/B/C browser manual verification 已通過；未修改 threshold、prompt 或 retrieval algorithm。 |
| 2.1.2 | PDF Index Result Chunk Count | done | 讓 Knowledge success card 顯示 backend 實際 indexed chunk count。 | Success state 顯示 filename、source kind、status、indexed/embedded chunk counts。 | Automated: Successful PDF indexing renders backend chunk count.<br>Manual: Upload PDF → verify deterministic count. | Backend count metadata 與 Knowledge success card 已通過 browser manual verification。 |
| 2.1.3 | PDF Source Inventory and Exact Duplicate Guard | done | 讓 Knowledge 看見 indexed PDF sources，並以 deterministic raw file hash 阻止 exact duplicate re-indexing。 | Indexed PDF source inventory、safe source metadata、owner/source-kind/raw-file-hash duplicate guard；不建立第二套 searchable chunks。 | Automated: Inventory、same raw-file-hash duplicate、same-name different-content、different-name same-hash regression passes.<br>Manual: Upload/index → inventory → exact duplicate result. | Backend/API、frontend automated tests、bounded local verification 與 browser manual acceptance 已完成；既有 `file_hash=NULL` records 未修改或刪除。 |
| 2.2 | URL Knowledge Pipeline | done | 以同一 generic pipeline 加入 URL。 | URL ingest、index、RAG、citation 與 Knowledge UI flow。 | Automated: URL ingest、retrieval、citation、frontend regression 與 build passes.<br>Manual: Paste URL → indexing → Chat → citation 已完成。 | Automated 與 frontend manual verification complete；不另建 source-specific contract。 |
| 2.3 | Screenshot / Image Knowledge Pipeline | done | 以 OCR 將 image 加入 generic Knowledge。 | Image upload、OCR、index、retrieval、citation 與 UI flow。 | Automated: OCR、image indexing/retrieval/citation、validation、dedup、API、frontend regression 與 build passes.<br>Manual: Upload image → OCR/index → Chat → citation 已完成。 | Automated 與 frontend manual verification complete；OCR failure 會 fail closed 並顯示可見 error。 |
| 2.3.1 | Multi-image Grouped Source UX | done | 讓一次 screenshot batch 成為一個 logical source，保留 ordered image parts、generic chunks 與 backend citation provenance。 | Multiple picker、natural ordering、最小 reorder、單一 `SourceDocument`、ordered image metadata、batch summary 與一筆 inventory source。 | Automated: One-source grouping、natural ordering、manual reorder、multipart fields、fixed sequence index、OCR completion-order regression、ordered-hash dedup、citation provenance、loading/duplicate-submit、inventory refresh 與 single-image regression passes.<br>Manual: Select multiple screenshots → confirm/reorder sequence → upload → verify one inventory row and per-image result/citation provenance 已完成。 | Frontend manual verification PASS；既有 single-image、PDF、URL behavior 保持不變。 |
| 2.3.2 | Screenshot Display Name + Preview | done | 以 ordered image 1 的 deterministic OCR content 提供 bounded screenshot title/preview，同時保留 filename provenance。 | `Screenshot · {title}` / `Screenshots · {title}` hierarchy、heading/sentence/fallback title、bounded OCR preview、grouped inventory rendering 與 backend image citation metadata。 | Automated: Heading-like title、meaningful sentence fallback、whitespace、bounded title/preview、usable OCR without heading 的 filename/timestamp fallback、empty OCR fail-closed、ordered-hash identity、inventory 不 dump raw OCR、image citation regression、single-image 與 PDF/URL regression passes.<br>Manual: Upload screenshot batch → verify title/preview hierarchy、single inventory row、image locator 與 original filename citation metadata 已完成。 | Frontend manual verification PASS；少量 OCR noise 屬可接受 limitation，不新增 LLM title-generation 或複雜 title classifier。 |
| 2.4 | YouTube Knowledge Pipeline | deferred | 以 transcript 將 YouTube 加入 generic Knowledge。 | YouTube URL、transcript、index、retrieval、citation 與 UI flow。 | Automated: Transcript and retrieval regression passes.<br>Manual: Paste YouTube URL → transcript/index → Chat → citation. | 維持 deferred priority，不阻塞目前 MVP closure。 |
| 2.5 | Notion UX Integration | deferred | 在既有 Notion index foundation 上補 deterministic Knowledge UX。 | `list pages`、select、sync、status、ask、citation 的 API 與 UI flow。 | Automated: Page listing, sync, permission and citation regression passes.<br>Manual: List pages → select → sync → ask → citation. | 維持 deferred priority，不阻塞目前 MVP closure。 |
| 3.0 | Conversation Sessions | done | 建立 session isolation 與 last-N short-term context。 | `ConversationSession`、`ConversationMessage`、last-N context、New Chat、URL session identity 與 session interaction。 | Automated: Backend full suite `696 passed, 5 skipped`; frontend `39 passed`; production build and migration checks pass.<br>Manual: Ask → follow-up → New Chat → old short-term context absent; browser verification PASS. | Synchronous request/response、session restore、ordering、isolation 與後續 3.0 verification follow-ups 已完成。 |
| 3.0.1 | Same-session Conversational Recall | done | 讓 bounded same-session conversation history 支援 conversational recall，同時維持 enterprise grounding boundary。 | Deterministic bounded recall mode、獨立 conversational prompt、previous user/assistant message、choice 與 recommendation reasoning recall；無 history 行為與 zero-citation contract。 | Automated: Conversation recall、authority boundary、session isolation、existing enterprise QA、provider failure、last-6/token budget regression passes.<br>Manual: Browser verify `hi` → `What did I say?`、previous answer choice、recommendation reasoning、New Chat isolation 與 enterprise unsupported claim。 | Browser core verification PASS。ConversationMessage 只作 conversational authority；KnowledgeChunk 仍是 enterprise evidence authority。部分 paraphrase recall 維持 limitation；broader intent selection deferred 到後續 bounded Agent runtime。 |
| 3.0.2 | Per-message Citation Persistence & Disclosure | done | 保存每則 grounded assistant message 的 backend citation metadata，並在 session restore 後以 per-message disclosure 顯示。 | Bounded citation metadata persistence、GET session/message citation contract、collapsed `Sources · N` disclosure；conversation-only recall 與 `insufficient_info` 不顯示 Sources。 | Automated: Citation persistence、atomic assistant message metadata、message attachment、session reload、frontend refresh、per-message disclosure、zero-citation recall/insufficient-info 與 existing session regression passes.<br>Manual: Grounded answer → next question → expand/collapse → refresh → session switch/restore，browser verification PASS。 | 已沿用既有 `ConversationMessage.metadata_json`，不需 migration。Per-message citation disclosure 與 backend authority boundary 已完成。`4.0 Persistent Memory` 維持下一個正式 implementation slice。 |
| 4.0 | Persistent Memory | done | 建立 explicit save 的 owner-scoped long-term memory。 | Explicit save、vector retrieval、owner scope、Memory Inspector、View/Delete。 | Automated: Save, owner filter, search and delete regression passes.<br>Manual: Session A save → New Chat → Session B recall → Inspector delete，browser verification PASS。 | Backend、frontend automated verification 與 browser manual verification 已完成。Memory 不混入 Knowledge corpus。 |
| 4.0.1 | Direct Saved-Memory Recall Coverage | done | 修正明確 saved personal、preference、decision、project context query 的 bounded deterministic memory routing。 | New Chat 後的中英文 direct recall query 進入 owner-scoped semantic LongTermMemory search，並保留 `used_saved_memory=true` 與 `citations=[]`。 | Automated: bounded personal question shapes、owner-scoped semantic search、memory priority、六個 name/occupation/decision/preference cases、non-memory enterprise fallback 與 full regression passes。<br>Process HTTP: occupation/name acceptance 與 negative control pass。<br>Manual: Session A save → New Chat → matching recall query → `Used saved memory`，browser verification PASS。 | 只使用 deterministic routing；不建立 general intent classifier、不使用 LLM routing、不搜尋所有 Knowledge 與 Memory、不改動 3.0 architecture。 |
| 4.0.2 | Saved-Memory Relevance Selection | done | 防止 direct recall 使用 unrelated saved memory，並讓無 sufficiently relevant memory 的 query fail closed。 | Direct/singular recall 只使用 best sufficiently-relevant memory；broad recall 保留 bounded top-k；relevance gate 通過 normalized cosine score。 | Automated: direct name/occupation selection、interest no-hit、broad bounded multi-memory、owner-scoped retrieval、zero enterprise citations 與 full regression passes。<br>Process HTTP: name、occupation、interest、broad live probe pass。<br>Manual: Session A 兩筆 memory → New Chat → direct、no-hit、broad queries，browser verification PASS。 | 使用 evidence-based deterministic floors；不加入 LLM reranker、semantic dedup、importance/temporal ranking、Agent loop、MCP 或 tool calling。 |
| 5.0 | MCP and Bounded Agent Runtime | done | 將 Knowledge、Memory 與 save capability 放入 bounded tool loop。 | `search_knowledge`、`search_memory`、`save_memory`、in-process MCP-compatible adapter、validation、max tool calls、termination。 | Automated: Allowlist, schema, permission, max 3 calls and termination regression passes.<br>Manual: Core bounded Agent browser acceptance verified. | 5.0 核心能力與 browser acceptance 已確認。剩餘 browser findings 已移至 5.0.3 與 5.0.4，不再阻塞 5.0 收尾。 |
| 5.0.1 | Agent Browser Verification Fixes | done | 修正 5.0 browser verification 發現的 explicit save、session draft isolation、retry duplicate、direct/broad memory recall 與 same-session conversational transform blockers。 | Backend-owned explicit save persistence、per-session in-memory draft、pending failed user-message retry reuse、Agent-selected bounded memory retrieval、clear memory intent deterministic fallback、saved-memory category filter、bounded conversation-only transform path；不新增 draft persistence、不修改 `ConversationMessage` schema、不開始 MCP protocol migration。 | Automated: Public conversation orchestration、tool-capable fake provider、frontend session switching、failure/retry、save policy、direct/broad/category memory context、provider-declined memory fallback、behavior-level transform、authority boundary 與 existing Knowledge/Memory routing regression passes。Workflow metadata 只記錄 bounded routing/retrieval 數值。<br>Manual: Explicit save、duplicate handling、Memory Inspector persistence、session draft isolation、same-session English to Chinese transform、New Chat isolation 與 response-language policy verified. | 5.0.1 核心 browser acceptance 已確認。Broad all-memory recall 與 implicit conversational transform 的剩餘問題改列為 5.0.3 與 5.0.4。 |
| 5.0.2 | Native MCP Protocol Integration | done | 將目前 in-process MCP-compatible adapter 對接 native MCP protocol boundary。 | Official Python MCP SDK、single-process stdio server、`initialize`、`tools/list`、`tools/call`、三個 allowlisted tools、trusted owner context 與既有 tool policy mapping。 | Automated: Native MCP client/server protocol tests、tool routing、permission、safe error mapping 與 full backend regression pass。<br>Manual: Native MCP initialize、allowlist discovery、knowledge / memory invocation、save fail-closed policy、owner / persistence spoof rejection 與 authorized subprocess save verified。 | 5.0.2 implementation 與 manual verification complete。MCP server 不重做 business logic；external client 沒有 trusted explicit-save context 時，`save_memory` 維持 fail closed。 |
| 5.0.3 | Semantic Memory Recall Hardening | done | 讓已明確保存的 memory 能以自然 paraphrase、縮寫與簡短 query 被找回。 | 保存原始 `content` 與 bounded `retrieval_text` 分離；embedding 使用 retrieval representation；Agent tool guidance 支援自然 project-memory question；保留 owner scope、relevance gate 與 explicit-save policy。 | Automated: 中英文自然 query、縮寫、cross-session recall、negative controls、原始 content preservation、owner/relevance/citation regression。<br>Manual: Browser positive recall、Memory Inspector original content、`Used saved memory` authority separation 與 unrelated-memory negative controls 均 PASS。 | Implementation 與 browser verification complete。本輪不加入 automatic memory extraction、general intent classifier、global threshold 放寬或大型 synonym dictionary。 |
| 5.0.3.1 | Generalized Semantic Memory Representation | deferred | 保留已驗證的 save-side semantic memory representation；未完成的 query-side generalization 暫不列入 current mainline。 | Current implementation 保留 user-authoritative original `content`、bounded derived `retrieval_text`、retrieval embedding、explicit-save authorization、strict bounded canonicalization fallback、original-content Inspector/API display、direct best-1、broad bounded multi-result、owner scope 與 relevance gates。 | Existing 5.0.3 automated 與 browser evidence：natural paraphrase recall、unrelated-memory negative control、original content preservation 與 authority separation。Deferred：`MemoryService.search_memories()` 的 query-side semantic normalization，以及 no-hit / low-confidence second-pass retry；不降低 global relevance floors、不建立大型 hard-coded synonym dictionary。 | 此 slice 不是整體失敗；save-side representation 已有 evidence。Query-side semantic normalization 尚未 implemented，後續若重啟再另行排程。 |
| 5.0.3.2 | Contextual Memory Application | manual_verification | 讓 saved company/project context 在能 materially improve Knowledge task 時，由 bounded Agent 組合 Knowledge 與 relevant memory。 | Bounded context requirement selection、`search_knowledge` + `search_memory`；contextual memory 使用 task-oriented query、bounded multi-result 與既有 relevance gate；Knowledge citation 與 `Used saved memory` authority 分離；SSE 顯示 bounded execution statuses。 | Automated: Knowledge-only、Memory-only、mixed contextual flow、relevant-memory filtering、no relevant memory、no automatic memory dump、owner isolation、max 3 tool calls、authority separation、SSE 與 Golden Set regression。<br>Manual: Browser verify A Knowledge-only、B Memory-only、C mixed contextual、D irrelevant memory fail-closed、E no memory dump；目前尚未人工驗證。 | Implementation complete，待 future manual verification。5.0.3.1 已 deferred；不新增 planner、intent classifier、tool、retrieval framework 或 memory automation。 |
| 5.0.3.2.1 | Contextual Memory Live-Provider Routing | manual_verification | 修正 live provider 在 `search_knowledge` 後因 Knowledge evidence 已足夠而直接 final，未重新評估 contextual saved memory 的 routing gap。 | Post-Knowledge bounded decision contract 保留 original user task、Knowledge evidence 與 `search_memory` availability；contextual provider query 不被 direct-recall metadata 覆寫；維持 Knowledge-only direct final、Memory-only direct recall、task-oriented query、authority separation 與最多 3 次 calls；SSE 不把 internal continuation decision 顯示成重複 `generating`。 | Automated: live-like provider behavior regression、post-Knowledge tool availability、direct-final negative control、contextual query preservation、memory type filter isolation、SSE status order、existing Agent/conversation/streaming regression。<br>Manual: 重新執行 5.0.3.2 mixed browser probes，確認 `Searching saved memory…`、`Used saved memory`、company size 與 development preferences 實際進入回答。 | Implementation complete，待 live-provider browser verification；不新增 keyword classifier、regex routing、planner、second Agent 或新 tool。 |
| 5.0.3.2.2 | Bounded Context Requirement Selection | manual_verification | 將這次回答需要的 Knowledge / Memory authority 變成 bounded structured decision，再由 backend deterministic 執行 required context acquisition。 | Typed `ContextRequirementDecision`、provider structured-output capability、schema validation、Knowledge → Memory bounded execution、分離 context assembly、無 tools 的單次 final generation、Memory no-hit fallback 與 enterprise `insufficient_info` authority；不加入 save decision、新 MCP tool 或 public API。 | Automated: `context-requirement-*` Golden Set 4 cases、Knowledge-only、Memory-only、mixed deterministic repeat、Memory no-hit fallback、Knowledge authority、owner scope、max 3 calls、malformed selector、provider wire validation、SSE 與 full regression。<br>Manual: 連續 3 次 mixed browser probe，確認 `Searching knowledge…` → `Searching saved memory…` → `Generating answer…`、Sources、`Used saved memory` 與 relevant company/development context。 | Implementation complete，待 future manual verification；5.0.3.1 已 deferred。 |
| 5.0.3.2.3 | Same-Session Context Requirement Stability | manual_verification | 修正同一 session 重送 substantive query 時，previous assistant answer 影響 authority selection，導致 Knowledge 未重新取得並錯誤進入 `insufficient_info`。 | Selector authority invariant：history 可協助理解 task，但 previous answer 不取代 current Knowledge evidence，previous saved-fact mention 不取代 current Memory retrieval；同一 substantive query 連續 3 次維持相同 required authorities；保留 conversational transform path。 | Automated: bounded selector contract、same-session repeated substantive query 3-turn Knowledge + Memory reacquisition、selector history input 與 existing transform regression；Manual: fresh session 同一 mixed query 連續 3 次，皆確認 Knowledge、Memory、Sources、`Used saved memory` 與非 `insufficient_info`。 | Implementation complete，待 browser manual verification；不開始 5.0.3.1 或 5.0.4。 |
| 5.0.3.2.4 | Pre-Final Knowledge Sufficiency Boundary | manual_verification | 建立 accepted Knowledge 的 pre-final gate，避免 provider sentinel 覆寫已完成的 Knowledge retrieval；whole-answer semantic sufficiency 仍是 `HYBRID`，Evidence Readiness 延後到 post-5.0.3.3。 | Bounded candidate、accepted evidence 與 assembled Knowledge counts；Knowledge-required 且無 accepted evidence 時回傳產品層 `insufficient_info`；已有 accepted Knowledge evidence 卻收到 provider sentinel 時回傳 `provider_contract_error`，並映射既有 `LLM_OUTPUT_INVALID` workflow failure；不修改 selector、history、prompt、retrieval ranking、threshold 或 context assembly behavior。 | Automated: raw candidate rejection、accepted Knowledge + provider sentinel contract failure、Knowledge + Memory no-hit success、Knowledge-missing fail-closed、memory-only、provider error、citation authority、explicit save、MCP 與 owner regression；Manual: future browser verification，讀取兩個 workflow metadata 並確認 pre-final Knowledge gate。 | Automated verification complete；不代表 whole-answer verifier 已完成；5.0.3.1 已 deferred。 |
| 5.0.3.2.5 | Knowledge Evidence Acceptance Gate | manual_verification | 讓 backend 分離 raw retrieval candidate、accepted Knowledge evidence、Knowledge context 與 grounded sufficiency，避免 top-k nearest chunk 被直接視為可回答 evidence。 | pgvector 使用 cosine distance 轉成 `[0,1]` normalized similarity；bounded candidate pool 為 `2 × top_k`、上限 20，先套用 inclusive `knowledge_relevance_floor=0.30` 再取 final `top_k`；lexical 既有 `score > 0` 行為不變；rejected Knowledge evidence 不進 context 或 citations；Knowledge-required 且零 accepted evidence 時 deterministic 回傳 `insufficient_info`；不修改 selector、history 或 Memory threshold。 | Automated: pgvector score semantics、positive/mixed/negative score inspection、below/equal floor、candidate/accepted/context counts、eligibility、zero-citation fail-closed、Knowledge-only、Knowledge + Memory、PDF/URL/Image、MCP 與 Golden Set regression；Manual: future browser verification，mixed query 應維持 Knowledge + Memory。 | Automated verification complete；5.0.3.1 已 deferred。 |
| 5.0.3.2.6 | Contextual Facet Retrieval Stability | manual_verification | 將 mixed contextual task 的 free-form Memory query 改為最多 2 個 bounded atomic contextual facets，讓每個 facet 直接成為 deterministic contextual Memory retrieval query。 | Typed `ContextualFacet` 與 mixed decision validation；Knowledge 一次加上最多 2 次 per-facet Memory search；保留 direct memory-only `memory_query` compatibility；partial/all Memory miss 時仍允許 accepted Knowledge 的 Knowledge-only synthesis；Memory authority、relevance floors、max 3 tool calls 與 SSE status contract 不變。 | Automated: facet schema、empty/long/extra-field rejection、mixed 3-call execution、repeated substantive query stability、partial/all Memory miss、Knowledge-missing gate、authority、max calls、SSE、Golden Set 與 existing Memory/Knowledge regression。Manual verification attempted; acceptance not passed. Findings are consolidated by 5.0.3.3. Browser cases A Knowledge-only、B Memory-only、C mixed contextual、D same-session mixed query 3 次、E contextual Memory miss。 | Implementation 與 automated verification complete；browser verification 前本 slice 維持 `manual_verification`。 |
| 5.0.3.2.7 | Final Synthesis Authority Isolation | manual_verification | 防止 previous assistant answer 進入 structured substantive final synthesis，讓同一 mixed query 每次都依 fresh Knowledge 與 Memory authority 重新產生答案。 | Selector 保留 bounded user/assistant history；final synthesis 只保留 bounded user-side context、current task、fresh Knowledge 與 fresh Memory。Conversation transform 仍可使用 previous assistant answer。Retrieval thresholds、facets、max 3 tool calls 與 SSE phase 不變。 | Automated: production API 同一 mixed query 連續 3 次、final request authority isolation、selector history、transform/recall、Knowledge-only、Memory-only、Memory miss、Knowledge missing、citation/owner/explicit-save/MCP/SSE 與 controlled replay harness regression。Manual verification attempted; acceptance not passed. Findings are consolidated by 5.0.3.3. Fresh New Chat 連續 3 次 mixed query，另驗證 Knowledge-only、Memory-only 與 conversational transform。 | Implementation 與 automated verification complete；`5.0.3.2.6`、`5.0.3.2.7` 與 parent `5.0.3.2` 等待 browser manual verification。 |
| 5.0.3.2.8 | Substantive Conversation Dependency Contract | manual_verification | 將 substantive task 是否需要前一個 completed conversation turn 的語義變成 typed selector contract。 | `ContextRequirementDecision.conversation_dependency` required enum；`none` 時 final synthesis 不帶 previous conversation，`required` 時 backend deterministic 提供最多一個同 session 最近 completed user/assistant pair 作 `CONVERSATION_REFERENCE_CONTEXT`。Conversation context 只作 reference interpretation，不成為 Knowledge、Memory、sufficiency 或 citation authority；transform、recall、direct Memory routing、facets、thresholds、max 3 calls 與 SSE phase 不變。 | Automated: strict schema、standalone none、四次 repeated substantive regression、selector history retention、required recent-pair selection、failed pending handling、reference authority boundary、missing Knowledge、transform、direct Memory、SSE、Golden Set `29/29` 與 existing regression。Manual verification attempted; acceptance not passed. Findings are consolidated by 5.0.3.3. Fresh New Chat mixed query 連續 4 次；Knowledge-only、Memory-only、transform；再驗證 referential follow-up 的 recent completed-turn boundary。 | Implementation 與 automated verification complete；`.6`、`.7`、`.8` 與 parent `5.0.3.2` 都維持 `manual_verification`。 |
| 5.0.3.3 | Context Authority Consolidation | deferred | 將 incidental raw conversation history 限制在 bounded reference resolution，讓 substantive retrieval 與 final synthesis 只使用 exact current user message、validated reference bindings 與 fresh authority context。 | Implemented D030 architecture：`Reference Binding` 只產生 backend 可驗證的 textual bindings，不建立 `conversation_dependency`、`reference_status`、`resolved_task` 或 free-form query rewrite。保留 explicit conversation recall/transform 與 direct Memory compatibility；保留 `ContextualFacet` 上限 2、Knowledge/Memory 分離、partial/all Memory miss degradation、max 3 tool calls 與 current pre-final sufficiency behavior。 | Automated verification complete：reference binding、authority boundary、dedicated conversation paths、contextual Memory、SSE、Golden Set 與 full regression。Unresolved actual-provider stability probe 與 browser acceptance deferred；不把 deterministic Golden Set PASS 視為 live-provider stability evidence。 | Architecture implementation 與 automated verification 已完成；unresolved live-provider stability 正式 deferred。`.6/.7/.8` findings 由本 slice 收斂，後續不列為 current mainline。 |
| 5.0.4 | Same-Session Conversational Transform Hardening | deferred | 修正已有 previous assistant answer 時，`用中文說`、`你簡單說` 等 implicit transform request 偶發落入 `insufficient_info` 的 browser follow-up。 | Same-session immediately previous assistant answer 支援 translate、simplify、rephrase、summarize；不需要新 Knowledge evidence、不呼叫 Knowledge/Memory tools、不產生 enterprise citations；New Chat fail closed。 | Automated: Implicit Chinese and English transform、same-session bounded context、no-tool/no-citation contract、New Chat isolation 與 unsupported enterprise claim regression。<br>Manual: Browser verifies language switch、simplify、rephrase、summarize 與 New Chat isolation。 | 維持 deferred，待後續 scope decision，不阻塞目前 MVP closure。 |
| 6.0 | SSE Streaming and UX Hardening | done | 將既有 Chat request/response 升級為 streaming lifecycle。 | Execution status、`answer_delta`、citations、`done` 與 error behavior。 | Automated: Full backend run `807 passed, 5 skipped`; frontend `54 passed`; production build、compileall 與 diff checks pass.<br>Manual: Browser manual verification PASS：`Searching knowledge…`、`Generating answer…`、progressive answer rendering 與 assistant message 的 `Sources · N` 均符合驗收要求。 | Frontend 由 Phase 1 延續，不重建。Provider 目前沒有 native token streaming；answer delta 使用 backend deterministic chunking。6.0.1 的 browser-visible rendering follow-up 已完成。 |
| 6.0.1 | Visible SSE Progressive Rendering | done | 讓 `generating` 與多個 `answer_delta` 在 browser 中分段可見。 | Final provider call 前的 `generating`、逐一 SSE completion yield、bounded multi-delta chunking、frontend render boundary 與 append semantics。 | Automated: Agent timing、SSE lifecycle、delta reconstruction、frontend event ordering 與 progressive rendering tests pass；full backend `807 passed, 5 skipped`；frontend `54 passed`。<br>Manual: Browser manual verification PASS：`Searching knowledge…` 與 `Generating answer…` 可見，answer progressive rendering，完成後 `Sources · N` 正確附屬於 assistant message。 | 不加入 provider-native token streaming，不修改 Agent policy、retrieval、memory、MCP 或 persistence contract。 |
| 6.0.2 | Independent Chat Pane Scrolling and Bottom Composer UX | done | 讓 Chat workspace 的 Global Navigation、Conversation Sessions 與 Current Conversation 各自擁有正確的 height / overflow ownership，並讓 Chat composer 固定在 bounded Chat pane 底部。 | App shell `100dvh` + hidden overflow；Global Navigation full-height no-scroll；session pane hidden overflow + independently scrollable session list；Chat pane 內的 conversation scroll area 同時包含 compact/empty hero、message history 與 citations；bottom composer、bounded auto-grow、restrained sidebar tokens、text-only brand、近底自動跟隨、上讀歷史時保留位置與 mobile drawer compatibility；Knowledge 與 Memory route surface 使用自己的 bounded vertical scroll。 | Automated: frontend `62 passed`；production build 與 `git diff --check` pass。<br>Manual: Browser final verification PASS：Global Navigation full-height、Conversation session list 與 Chat conversation 可獨立 scroll；Knowledge / Memory 在 desktop 100% zoom 可向下 scroll；compact bottom composer 留在 Chat pane 底部；不需要 browser zoom out。 | Chat workspace 與 Knowledge/Memory route scroll 仍是本 slice；未修改 SSE、Agent、Memory、Knowledge 或 backend contract。Explicit save streaming follow-up 另列 6.0.3。 |
| 6.0.3 | Explicit Save Streaming Regression | done | 修正 tool-capable Agent path 中，provider 的合法 `save_memory` type 與 backend explicit-save classification 不一致時，明確 save request 被錯誤終止的問題。 | 保留 orchestrator 的 explicit-save authorization、backend 分類的 memory type/content、MemoryService embedding/persistence、duplicate status、Memory Inspector 與 SSE `saving_memory` → `done` mapping；provider arguments 維持 schema validation，但不得覆寫 trusted explicit-save context。 | Automated: exact company save streaming、trusted type/content persistence、cross-session recall、duplicate、ordinary statement rejection、Knowledge/citation/SSE regression；backend `799 passed, 5 skipped`；Native MCP targeted regression `tests/test_native_mcp_protocol.py`：`.venv/bin/python -m pytest -q`，`10 passed`；frontend `62 passed`；production build 與 `git diff --check` pass。<br>Manual: Browser final verification PASS：explicit save、Memory Inspector persistence、New Chat cross-session recall、`Used saved memory`、duplicate save 與 ordinary statement behavior。 | 由 6.0 browser verification 的 explicit-save streaming failure 產生；未修改 backend API contract、SSE protocol、Knowledge retrieval 或 7.0。 |
| 7.0 | Evaluation and Demo Hardening | manual_verification | 將 Agent behavior 與正式 demo 變成可重複 regression，作為下一個唯一主線 priority。 | 29-case Agent Golden Set、deterministic runner、safe demo preflight、5 至 10 分鐘 demo flow、README/doc alignment。 | Automated: Golden Set `29/29` pass；focused evaluation/preflight tests pass。<br>Manual: Complete the formal browser Demo Story。 | `7.0` 是下一個唯一主線 priority。5.0.3.1 未完成部分與 5.0.3.3 unresolved live-provider stability 已 deferred，不阻塞本線。技術 hardening 會先經過 8.x，再回到正式 browser Demo Story。 |
| 8.0 | Retrieval Evaluation Foundation | done | 在不改變 retrieval behavior 的前提下，建立 production retrieval quality 的 controlled evaluation foundation。 | Repository inspection、current parser/chunker/embedding/pgvector path inventory、pilot contract 與 benchmark boundary。 | Automated: Existing Agent Golden Set `29/29` pass；inspection/spec foundation reviewed。<br>Manual: Documentation and scope reviewed. | 本 slice 不代表 retrieval quality 已被證明；未修改 runtime、schema、dependency 或 `eval/golden_set.yaml`。 |
| 8.1 | Pilot Retrieval Benchmark | done | 以三份 frozen PDF、22 個 manually reviewable cases 與 current production retrieval path 建立第一個 retrieval-only baseline。 | Corpus manifest、YAML schema/loader、macro-by-case Recall@1/3/5、MRR、full-case success、source/page coverage、per-case evidence-group diagnostics、negative rejection、focused tests。 | Automated: Loader、schema、annotation preflight、metrics、bounded report、deterministic corpus identity 與 retrieval-only runner tests pass。<br>Manual: 22 個 PDF gold anchors 已 review；isolated PostgreSQL+pgvector pilot 與 bounded threshold calibration 已完成。 | Current retrieval baseline measured；pilot annotations human reviewed；threshold calibration completed；production retrieval unchanged。Hard-negative behavior 另由 grounding verification 確認，不再做 scalar threshold tuning。 |
| 8.2 | Positive Retrieval Quality | done | 以小型 positive Gold expansion 與單一 embedding alternative A/B，檢查 semantic paraphrase 與 multi-evidence coverage 的 retrieval quality。 | 30-case benchmark、20 positive / 10 hard-negative、4 semantic paraphrase、4 multi-evidence、current baseline、ONE embedding A/B 與 bounded comparison。 | Automated: Gold preflight、focused retrieval/QA regression、Agent Golden Set、single embedding A/B controls 與 current positive failure-map consistency pass。<br>Manual: `rv-023` 至 `rv-030` Gold review、8.2 execution 已完成；A/B adoption verification pending provenance audit。 | 8.2 slice 已完成；production 維持 `text-embedding-3-small / 1536`，未修改 retrieval behavior。Earlier A/B aggregate 結果缺少可重建的 bounded per-case artifact，adoption decision 需 revalidation；本輪不新增 embedding 或 chunking experiment。 |
| 8.3 | Source-to-Chunk Fidelity Audit | done | 檢查 source fidelity、parser normalization 與 chunk boundary 是否形成 retrieval failures 的 root-cause pattern。 | Visual source review、deterministic parser/chunker inspection、frozen corpus verification、focused parser/chunker tests 與 parser A/B gate decision。 | Automated: focused parser/chunker tests、frozen corpus checks 與 audit evidence pass。<br>Manual: source/page review 與 parser A/B gate reviewed。 | `UPSTREAM_SUPPORTED=0`、`UPSTREAM_PLAUSIBLE=1`、`UPSTREAM_NOT_SUPPORTED=6`；parser A/B 不需要。Week 3 p10 的 `==` / `<=` extraction corruption 是 parser fidelity risk，但不是七個 retrieval failures 的共同 root cause；不啟動 parser migration。 |
| 8.4 | Evidence Readiness | automated_verified | 在 accepted Knowledge evidence 與 final synthesis 之間建立 backend-owned pre-final readiness gate，避免 partial evidence 直接產生 unsupported complete answer。 | `EvidenceReadinessDecision`、共用 readiness provider/helper、structured Agent path 與 `/api/qa` gate、`qa_answer_v4`、fail-closed semantics 與 backend citation authority。 | Automated: readiness schema、authority boundary、single/complete/partial evidence、zero-evidence bypass、Memory boundary、provider/contract failure、Agent runtime、QA orchestrator、citation、SSE 與 impacted regression suites pass。<br>Actual-provider: Knowledge-only positive probes `rv-001`、`rv-002`、`rv-010` pass；incomplete/negative safety probes `rv-009`、`rv-027`、`rv-029`、`rv-020` safe；controlled mixed Knowledge + Memory 仍為 `PERSISTENT_MIXED_KNOWLEDGE_READINESS_FALSE_NEGATIVE`。<br>Manual: 尚未進行 frontend/browser verification。 | Production retrieval freeze 與 Knowledge/Memory authority separation 維持不變；`accepted_evidence_count > 0` 不代表 answer ready。Deterministic implementation 與 automated verification complete；mixed actual-provider semantic stability unresolved and deferred，不阻塞其他 MVP work，也不建立 `8.4.1`。 |
| 6.0.3.1 | Deterministic Explicit Save Command | done | Public conversation 在 backend 產生有效 `ExplicitSaveIntent` 後，統一使用 trusted `MemoryService` 執行 persistence，不再由 provider tool selection 決定。 | Public API/SSE A、B、C parity、provider call count、duplicate、save failure、ordinary statement、6.0.3 safeguard 與 Native MCP regression。Backend `958 passed, 6 skipped`；frontend `62 passed`；production build、compileall、`git diff --check` 與 browser acceptance pass。 | 不修改 parser、Knowledge、Memory retrieval threshold 或 frontend event mapping。Exact C browser first-save、duplicate-save 與 deterministic `saving_memory` behavior 已完成驗證。Mixed explicit-save 加 substantive task 維持 unsupported/deferred。 |

ID 規則：`N.0` 是 major capability，`N.M` 是 feature slice 或 follow-up，`N.M.K`
是驗證後新增的更細 follow-up。新工作直接 append，不能為了排序重編既有 ID。

Status vocabulary：`planned`、`in_progress`、`automated_verified`、
`manual_verification`、`done`、`blocked`、`deferred`。

- `5.0.3=done`；`5.0.3.1=deferred`；`5.0.3.2=manual_verification`。
- `5.0.3.2.6`、`5.0.3.2.7`、`5.0.3.2.8` 仍為 `manual_verification`，並保留為 consolidation 前的 diagnostic iterations；其 findings 由 `5.0.3.3` 收斂。
- `5.0.3.3=deferred`，architecture implementation 與 automated verification complete；unresolved live-provider stability deferred；`5.0.4=deferred`。
- `7.0=manual_verification`，為下一個唯一主線 priority；先完成 formal browser Demo Story，再依 evidence 決定後續 work。
- `8.3=done`；source-to-chunk audit 已完成，parser fidelity risk 不形成目前 failures 的共同 root cause。
- `8.4=automated_verified`；Evidence Readiness implementation 與 automated regression complete。Knowledge-only positive 與 incomplete/negative safety live evidence 已具備，但 controlled mixed Knowledge + Memory actual-provider acceptance 仍 unresolved；後續 semantic-stability work deferred，不建立 `8.4.1`。
- `6.0.3.1=done`；Public explicit save deterministic backend path 與 browser acceptance 已完成；mixed explicit-save 加 substantive task 維持 unsupported/deferred。

## Post-5.0.3.3 Planned Directions

以下方向只記錄 post-`.3` planning，不建立 `.3.1`、`.3.2`、`.3.3`、`.3.4 implementation IDs：

| Direction | Scope |
| --- | --- |
| Evidence Readiness | `8.4` 已完成 backend implementation 與 automated verification。`knowledge_relevance_floor=0.30` 仍只代表 chunk relevance acceptance；current semantic sufficiency authority 是 backend gate 加上 bounded readiness provider。Frontend/browser verification pending。 |
| Final Synthesis Contract Hardening | Evidence Readiness freeze 後，再評估 insufficient response 是否由 backend deterministic 產生，以及 sufficient path 是否只允許 final answer-only synthesis；屆時才考慮移除 final `INSUFFICIENT_INFO` semantic veto 與採用 structured answer-only final schema。 |
| Selector Authority Slimming | 後續檢查 `needs_knowledge`、`needs_memory` 與 `memory_query` 哪些可由 route、`contextual_facets` 或既有 dedicated path deterministic derive，減少由 LLM 重複決定的 control bits。 |
| Live Semantic Stability Gate | 建立 release-level actual-provider evaluation，例如同一 self-contained mixed query 執行 10 次，要求 10/10 completed、0 Request failed、0 `provider_contract_error`、0 unexpected `insufficient_info`、無 incidental raw history 進入 final，並穩定取得 Knowledge 與 Memory authority。Level 1 deterministic CI、Level 2 Golden Set/controlled replay、Level 3 actual-provider stability 三層都保留；本方向 deferred，不是 7.0 的前置條件。 |
